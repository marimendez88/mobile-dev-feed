import { Option } from '@/models/';

export const userSelection: Option[] = [
  { name: 'Ionic Blog', url: 'https://ionicframework.com/blog/feed/' }
] as Option[];

export const feedStoreName = 'FEED_STORE';