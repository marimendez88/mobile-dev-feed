import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile-dev-feed',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
